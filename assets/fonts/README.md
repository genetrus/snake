# Fonts

SDL_ttf loads TTF/OTF font files from this folder. Add an OFL-licensed font here (for example: `Inter-Regular.ttf`, `Inter-Bold.ttf`).

Codex UI/PR views may not show binary diffs, so add the font files locally (e.g., via PowerShell and `git add`) after the PR is created.
